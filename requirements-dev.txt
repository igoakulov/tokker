# Development dependencies for Tokker
# These are not required at runtime and should be installed for development, linting, testing, building, and publishing.

# Build and publish
build>=1.0.0
twine>=5.0.0
setuptools>=61.0
wheel>=0.42.0

# Formatting and linting
black>=24.0.0
ruff>=0.4.0

# Type checking (optional but recommended)
mypy>=1.8.0
types-setuptools>=69.0.0.20240125

# Testing and coverage
coverage>=7.5

# Project management / environment
uv>=0.1.0
